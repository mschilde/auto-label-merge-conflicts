import { run } from '../index';

describe('Auto label merge conflicts', () => {
  describe('Parameters', () => {
    test('should require CONFLICT_LABEL_NAME', () => {
      console.log(run);
      expect(1).toBe(1);
    });
  });
});
